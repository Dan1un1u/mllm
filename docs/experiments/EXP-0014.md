# EXP-0014 — QHPI in-graph U8xS8 HMX feasibility gate

## Hypothesis

QAIRT 2.49 QHPI can execute a custom asymmetric-U8 by signed-S8 matrix product on the V79 integer HMX resource while keeping the operator boundary inside one QNN graph and its internal tensors in TCM.

## Primary physical variable

Replace one fixed-shape native QNN integer MatMul implementation with a QHPI custom U8xS8 HMX implementation; keep shape, integer operands, quantization parameters, output contract, toolchain, hardware, and surrounding graph fixed.

## Hard gate

The micrograph must compile and execute on PJZ110/V79 in one QNN graph; the QHPI descriptor must request HMX and TCM-only internal placement; the custom operator must have zero operator-attributed DDR read/write and no spill/fill or CPU fallback; Optrace must identify its execution; and requantized U8 output must match the independent asymmetric integer reference exactly or within a predeclared one-LSB tolerance. Any failure stops before fused attention.

## Evidence profile

micrograph-performance

## Outcome

Local gate passed with valid evidence; project adoption remains pending.

- Source: `codex/exp-0014-qhpi-u8-hmx-feasibility` at
  `e47c775c70d5eb009489b84772cca036630cac32`
- QAIRT/target: `2.49.0.260730`, PJZ110 / SM8750 / HTP V79
- Fixed tile: asymmetric U8 `[1,8,8,32]` by symmetric S8
  `[1,1,32,32]`, producing asymmetric U8 `[1,8,8,32]`
- True-HMX proof: Optrace reports `kernel_resources=HMX`, HMX trace lane,
  and `op_flags=uses_hmx`; DSP disassembly contains `activation.ub`,
  `weight.b`, channel-major `:cm`, and saturated U8 store instructions
- Placement proof: the custom node reports DRAM read/write `0/0` and VTCM
  read/write `4096/2048`; no spill, fill, CPU fallback, or graph split was
  observed
- Correctness: identity, signed-permutation, and structured inputs each ran
  twice on target with exact integer-reference equality (`mismatches=0`)
- Recorded custom-node work/active/dominant cycles: `128952`; performance was
  not a gate for this feasibility experiment
- Evidence:
  `D:\llm_exp\results\exp0014_qhpi_u8_hmx_feasibility_20260824`
- Context and schematic:
  `D:\llm_exp\models\exp0014_qhpi_u8_hmx_probe\20260824-attempt3`

The required QNN Crouton8 ↔ HMX bridge uses channel-major HMX loads and
stores, so it does not need an HVX layout-conversion pass or a DDR round trip.
The logical S8 graph input is presented to QHPI as offset-128 QUInt8 physical
storage, so this feasibility implementation still stages 1024 weight bytes in
TCM before HMX. The kernel is fixed-shape and does not implement or authorize
QK→Softmax→AV fusion; that would require a separate approved experiment.
