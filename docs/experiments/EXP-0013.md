# EXP-0013 — W4A16 current-source regression

The archived W4A16 model was run through the current QAIRT 2.49 source and
profiling stack to check that recent framework/output work had not silently
broken the usable reference. The final run reproduced the prior 77/100
short-answer result with zero recorded mismatch. Its speed samples were
thermal-contaminated and are retained only for compatibility diagnosis, not as
the Performance Comparator.

- Source: `codex/w4a16g32-repro-baseline` at `462c9207`
- Evidence:
  `D:\llm_exp\results\qwen3_sm8750_v79_w4a16_current_regression_qairt249_20260824_130226`
- Adoption: `not_applicable` diagnostic
